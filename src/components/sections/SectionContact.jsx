import { Container } from '../Container';
import { Title } from '../Title';
export const SectionContact = ({ id }) => {
    return (
        <div id={id}>
            <Container>
                <section>
                    <Title>CONTACT</Title>
                </section>
            </Container>
        </div>
    );
};
